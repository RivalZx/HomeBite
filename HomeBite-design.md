# HomeBite 家庭点餐小程序设计文档 v0.2

> **更新说明**：v0.1 → v0.2 根据开发实际情况同步。v0.1 中的部分设计因 MVP 范围调整暂未实现，已更新状态。

---

## 1. 产品定位

HomeBite 是一个面向家庭内部使用的点餐小程序。核心场景是：家庭成员在小程序中浏览「吃面 / 吃米 / 大餐」分类，查看每道菜的照片和名称，点选今天想吃的菜品加入心愿单，提交生成点餐记录并通过云数据库持久化。

整体气质柔和、可爱、轻松，像家庭冰箱上的手写菜单。

---

## 2. 视觉方向 ✅ 已实现

### 2.1 关键词

柔和 / 可爱 / 家庭感 / 清爽 / 易点选

### 2.2 色彩 ✅ 全部实现

| 用途 | 色值 | 使用位置 |
|------|------|---------|
| 背景色 | `#FFFDF8` | 页面背景、导航栏 |
| 主按钮蓝 | `#A9D8F5` | 选中分类、提交按钮、小勾 |
| 分类绿 | `#B6F0C0` | 未选中分类按钮 |
| 轮播粉 | `#FFC5C9` | 角标、空状态轮播底色 |
| 深色 | `#222222` | 主文字 |
| 辅助色 | `#666666` | 次要文字 |
| 淡黄 | `#FFE88A` | 轮播底色（备用） |

### 2.3 图形风格 ✅ 已实现

- 大圆角（菜品卡片 22rpx、按钮 16-48rpx）
- 柔和卡片色块，无描边
- 简洁线条风图标（emoji 代替）
- 菜品无图时用 emoji + 色块占位

---

## 3. 首页结构 ✅

首页从上到下：

```
┌──────────────────────────┐
│  🏠 兜兜家        📋记录  │  ← 顶部栏
├──────────────────────────┤
│   [  轮播卡片（菜品照片） ] │  ← 有图显示图，无图显示提示
├──────────────────────────┤
│  🍜吃面！ 🍚吃米！ 🍗大餐！ │  ← 分类切换
├──────────────────────────┤
│  [菜品1] [菜品2] [菜品3]  │
│  [菜品4] [菜品5]  [+  ]  │  ← 3列网格，末尾+号
├──────────────────────────┤
│                          │
├──────────────────────────┤
│  ❤ 心愿单    [提交点餐]   │  ← 底部固定栏（含数量角标）
└──────────────────────────┘
```

---

## 4. 页面模块说明

### 4.1 顶部照片轮播区 ✅ 已实现

**用途**：展示已上传的菜品照片，自动轮播。

**实现**：
- 自动从菜品列表中筛选有图菜品构建轮播
- 使用微信 `<swiper>` 组件，支持左右滑动
- 显示分页圆点、自动播放（4s 间隔）
- 左右露出上一张/下一张预览
- 无图时显示默认提示轮播

**待确认（未实现）**：
- ~~手动添加照片到轮播~~（当前仅从菜品图片自动构建）
- ~~点击查看大图~~

### 4.2 分类切换区 ✅ 已实现

三个固定分类：`🍜 吃面！` / `🍚 吃米！` / `🍗 大餐！`

**实现**：
- 横向等宽按钮，选中蓝色、未选绿色
- 点击切换后刷新下方菜品网格
- 切换分类**不会**清空已选菜品

### 4.3 菜品卡片网格区 ✅ 已实现

**布局**：3 列网格，卡片为方形大圆角

**卡片内容**：
- 有图：显示菜品图片（`mode="aspectFill"`）
- 无图：显示分类 emoji（🍜/🍚/🍗）搭配浅底色
- 菜名显示在图片下方

**交互**：
- 点击：选中/取消选中（边框变蓝 + 右上角 ✓ 角标）
- 长按：进入编辑页（修改名称/图片/分类/删除）
- 选中态：蓝色边框 + 蓝色圆勾

**已知限制**：
- ~~图片非必填~~ → 当前图片非必填，无图显示 emoji + 色块

### 4.4 添加菜品按钮 ✅ 已实现

**位置**：菜品网格末尾

**样式**：圆角虚线框 + 大 `+` 号 + "加一道"文字

**点击后**：跳转新增菜品页（`add-dish` 页面），包含：
- 上传/拍摄照片（选填，不上传也可以保存）
- 菜名（必填，空时保存按钮不可用）
- 分类（默认使用当前所在分类，支持手动切换）
- 保存/取消

**实现细节**：
- 照片上传到微信云存储，返回 `cloud://` 格式 URL
- 云存储不可用时跳过图片，不影响菜品保存
- 分类默认跟随当前浏览分类

### 4.5 滚动浏览区 ✅ 已实现

- 首页整体上下滑动
- 菜品网格随页面自然延展
- 底部操作区 `position: fixed` 固定在底部
- 无"没有更多"提示

### 4.6 底部操作区 ✅ 已实现

| 按钮 | 功能 | 状态 |
|------|------|------|
| ❤ 心愿单 | 显示已选数量角标，点击进入心愿单页 | ✅ |
| 提交点餐 | 提交前确认弹窗，提交后 toast 提示 | ✅ |

**实现**：
- 未选菜时提交按钮置灰（`opacity: 0.35`）
- 已选菜后心愿单显示红色数量角标
- 提交前弹出 `wx.showModal` 二次确认
- 提交成功 toast "已告诉大厨啦 🎉"

### 4.7 微信订阅通知 ❌ 未实现

**当前状态**：v1.0 未实现。提交后仅显示成功 toast，不发送微信消息。

**计划**：后续版本通过云函数 + 微信订阅消息模板实现。

---

## 5. 页面状态 ✅ 全部实现

| 状态 | 实现 |
|------|------|
| 默认状态 | 轮播 + 分类 + 菜品网格 |
| 空分类状态 | 显示"这里还没有菜菜，先加一道吧" + 加菜按钮 |
| 加载状态 | "加载菜谱中..." |
| 已选择状态 | 菜品边框变蓝 + ✓ 角标 |
| 提交成功状态 | toast "已告诉大厨啦 🎉" |
| 云加载失败 | 自动降级到默认菜品/本地存储数据 |

---

## 6. 页面清单

### 6.1 首页（index）✅

用于浏览和选择菜品。路由：`pages/index/index`

### 6.2 新增/编辑菜品页（add-dish）✅

路由：`pages/add-dish/add-dish`

**字段**：
- 菜名（必填，maxlength=20）
- 分类（必填，noodle/rice/feast 三选一）
- 图片（选填，云存储上传）
- 备注（选填）

**编辑能力**：修改菜名、更换图片、修改分类、删除菜品

**不支持**：~~拍照识别菜名、~~ ~~是否常吃标记~~

### 6.3 心愿单页（wishlist）✅

路由：`pages/wishlist/wishlist`

**功能**：
- 展示已选菜品列表（有图显图，无图显 emoji）
- 单条删除（✕ 按钮）
- 填写备注（"跟大厨说两句～"）
- 确认提交

### 6.4 历史记录页（history）✅

路由：`pages/history/history`

**功能**：
- 按时间倒序展示所有点餐记录
- 每张卡片显示：日期 + 周几 + 时间 + 菜品标签 + 状态（待做/已完成）
- 有备注时显示备注内容（粉色左边框）
- 空状态："还没有点餐记录"

**日期显示**：
```
┌──────────────────────┐
│  ┌────┐              │
│  │ 21 │  周二        │
│  │ 6月│  15:30   待做 │
│  └────┘              │
│  红烧排骨 蛋炒饭 水煮鱼  │
└──────────────────────┘
```

---

## 7. 技术架构

### 7.1 整体方案

```
[小程序前端] ←→ [微信云开发]
                    ├── 云数据库（dishes / orders / family_members）
                    └── 云存储（菜品图片）
```

### 7.2 数据存储

| 数据 | 存储方式 | 备注 |
|------|---------|------|
| 菜品 | 云数据库 `dishes` 集合 | 每次首页加载时拉取 |
| 订单 | 云数据库 `orders` 集合 | 提交时写入 |
| 菜品图片 | 云存储 | 上传后获得 `cloud://` URL |
| 本地降级 | `wx.setStorageSync` 前缀 `hb_` | 云服务不可用时自动降级 |

### 7.3 容灾机制

所有云操作（读取/写入/上传）均带有 `catch` 降级：
- `fetchDishes` 失败 → 使用本地存储数据或默认数据
- `addDish` 失败 → 写入本地 `hb_dishes`
- `fetchOrders` 失败 → 读取本地 `hb_orders`
- `submitOrder` 失败 → 写入本地 `hb_orders`
- 图片上传失败 → 清空图片路径，不影响菜品保存

### 7.4 图片处理流程

```
用户选择图片 → 临时路径（wxfile://）
  → wx.cloud.uploadFile() 上传到云存储
  → 获得 cloud:// 永久链接
  → 保存到数据库
  → 加载时通过 getTempFileURL() 转为 HTTP 临时链接
```

---

## 8. 数据模型（实际实现）

### 8.1 菜品 Dish（云数据库 `dishes` 集合）

| 字段 | 类型 | 说明 |
|------|------|------|
| `_id` | 自动 | 云数据库自动生成 |
| `familyId` | string | 默认 `'default'` |
| `name` | string | 菜名 |
| `category` | string | `noodle` / `rice` / `feast` |
| `imageUrl` | string | 云存储 URL 或空 |
| `note` | string | 可选备注 |
| `sortOrder` | number | 排序（当前未使用） |
| `createdAt` | Date | 创建时间 |

### 8.2 点餐记录 Order（云数据库 `orders` 集合）

| 字段 | 类型 | 说明 |
|------|------|------|
| `_id` | 自动 | 云数据库自动生成 |
| `familyId` | string | 默认 `'default'` |
| `items` | array | `[{name, category}]` |
| `note` | string | 订单备注 |
| `createdBy` | string | 用户 openid（预留） |
| `status` | string | `'pending'` / 其他 |
| `createdAt` | Date | 提交时间 |

### 8.3 家庭成员 FamilyMember（云数据库 `family_members` 集合）

| 字段 | 类型 | 说明 |
|------|------|------|
| `openid` | string | 微信用户标识 |
| `familyId` | string | 家庭 ID |
| `role` | string | `'member'`（预留） |
| `joinedAt` | Date | 加入时间 |

### 8.4 前端本地数据结构

**app.js globalData**：
```js
dishes: [
  { id, name, category, imageUrl, note, selected }
]
selectedDishes: [
  { id, name, imageUrl, category, note }
]
orders: []  // 从云数据库加载
```

---

## 9. 云函数清单

| 函数名 | 状态 | 说明 |
|--------|------|------|
| `login` | ✅ 已部署 | 微信登录，自动加入默认家庭 |
| `uploadImage` | ✅ 已部署 | 图片上传中转（实际由前端直调 SDK） |
| `seedDB` | ✅ 已部署 | 初始化默认菜品数据（可选） |

---

## 10. 已实现功能 vs 设计文档对比

| 功能 | 设计文档要求 | 实现状态 |
|------|------------|---------|
| 轮播展示菜品照片 | 猫咪/推荐/照片 | ✅ 自动从菜品图片构建 |
| 三个分类切换 | 吃面/吃米/大餐 | ✅ |
| 菜品网格 3 列 | 方形卡片 | ✅ |
| 选中取消选中 | 边框+小勾 | ✅ |
| 图片+菜名显示 | 图片+名称必填 | ✅ 图片选填，无图有兜底 |
| 长按编辑菜品 | 编辑/删除 | ✅ |
| + 新增菜品 | 加号按钮 | ✅ |
| 新增分类默认当前 | 跟随当前分类 | ✅ |
| 上下滚动浏览 | 自然延展 | ✅ |
| 底部固定操作区 | 心愿单+提交 | ✅ |
| 心愿单角标 | 已选数量 | ✅ |
| 提交确认弹窗 | 二次确认 | ✅ |
| 提交成功提示 | 成功反馈 | ✅ |
| 心愿单页 | 查看/删除/提交 | ✅ |
| 历史记录 | 按日期查看 | ✅ |
| 历史日期展示 | 月/日/周几/时间 | ✅ |
| 备注输入 | 可选备注 | ✅（心愿单页） |
| 微信订阅通知 | 消息推送 | ❌ **未实现** |
| 微信登录 | openid 获取 | ⚠️ 已部署云函数但前端未集成 |
| 多家庭支持 | 数据模型预留 | ⚠️ familyId 已预留但前端只做单家庭 |
| 角色权限 | owner/chef/editor/member | ❌ 未实现 |
| 分享邀请家庭成员 | 邀请码/分享卡片 | ❌ 未实现 |
| 空分类状态 | 加菜引导 | ✅ |
| 加载状态 | 加载提示 | ✅ |
| 云服务降级 | 本地存储兜底 | ✅ |
| 图片加载失败兜底 | — | ✅ binderror 回退 emoji |

---

## 11. MVP 边界（v1.0 范围）

### 已实现
- 首页菜品展示 · 分类切换
- 新增菜品（照片选填 + 菜名必填）
- 长按编辑/删除菜品
- 点选"想吃" / 取消
- 心愿单确认（含删除+备注）
- 提交生成点餐记录（云数据库）
- 历史记录查看（含日期展示）
- 11 道默认菜品开箱即用
- 云存储图片上传与加载
- 云服务故障时的本地降级

### 不包含（后续版本）
- 微信订阅消息通知大厨
- 家庭成员登录与身份识别
- 多家庭/多空间支持
- 角色权限体系
- 分享邀请加入家庭
- 轮播图手动管理
- 图片点击查看大图
- 一键再来一单
- 菜品排序管理
- "是否常吃"标记

---

## 12. 项目文件结构

```
D:\work\HomeBite\
├── project.config.json           ← 项目配置（AppID + 云开发环境）
├── HomeBite-design.md            ← 本文档
├── miniprogram/                  ← 小程序代码
│   ├── app.js                    ← 入口 + 云初始化 + 数据操作
│   ├── app.json
│   ├── app.wxss                  ← 全局样式
│   ├── sitemap.json
│   └── pages/
│       ├── index/                ← 首页（轮播+分类+菜品网格+底部操作）
│       ├── add-dish/             ← 新增/编辑菜品（拍照+菜名+分类）
│       ├── wishlist/             ← 心愿单（列表+备注+提交）
│       └── history/              ← 历史记录（日期+菜品+状态）
├── cloudfunctions/               ← 云函数
│   ├── login/                    ← 微信登录
│   ├── uploadImage/              ← 图片上传
│   └── seedDB/                   ← 初始化菜品数据
└── photos/                       ← 本地菜品图片备份
```

---

> **最后更新**：2026-06-21 | **版本**：v0.2
> **开发环境**：微信小程序 + 云开发（数据库 + 存储）
> **项目状态**：v1.0 MVP 已完成，可在家庭内部使用
