# HomeBite 家庭点餐小程序设计文档 v0.1

## 1. 产品定位

HomeBite 是一个面向家庭内部使用的点餐小程序。核心场景是：家庭成员在小程序中浏览「吃面 / 吃米 / 大餐」分类，查看每道菜的真实照片和名称，点选今天想吃的菜品加入心愿单，提交后通过微信订阅消息通知负责做饭的大厨。

整体气质需要柔和、可爱、轻松，像家庭冰箱上的手写菜单，而不是正式外卖 App。

## 2. 视觉方向

### 2.1 关键词

- 柔和
- 可爱
- 家庭感
- 手绘感
- 清爽
- 易点选

### 2.2 色彩

基于 UI 草图，主色系使用低饱和的马卡龙色：

- 背景色：`#FFFDF8` 或接近白色的暖白
- 主按钮蓝：`#A9D8F5`
- 分类绿：`#B6F0C0`
- 轮播粉：`#FFC5C9`
- 线条深色：`#222222`
- 辅助文字：`#666666`
- 强调点：可少量使用浅黄 `#FFE88A`

颜色使用原则：

- 不使用高饱和色。
- 按钮和卡片保持柔和色块。
- 黑色描边可以保留手绘感，但实际开发中建议使用略柔和的深灰，避免过硬。

### 2.3 图形风格

- 组件边角使用大圆角。
- 卡片和按钮可使用轻微描边，模拟手绘框线。
- 图标优先使用简洁线条风。
- 可加入 HomeBite 猫咪 logo 作为品牌元素，但不要喧宾夺主。

## 3. 首页结构

首页是点餐主页面，从上到下分为：

1. 顶部照片轮播区
2. 菜品分类切换区
3. 菜品卡片网格区
4. 添加菜品按钮
5. 滚动浏览区
6. 底部操作区

## 4. 页面模块说明

### 4.1 顶部照片轮播区

用途：展示家庭菜品照片、猫咪照片、今日推荐或用户手动上传的图片。

布局：

- 位于页面顶部。
- 卡片容器使用大圆角。
- 中间主图作为视觉主体，铺满轮播容器的上下空隙。
- 左右两侧露出上一张/下一张图片预览。
- 两侧预览图半透明，只做提示，不能抢主图焦点。
- 下方显示分页圆点。

交互：

- 支持左右滑动切换。
- 轮播切换时，当前主图滑到左侧成为半透明上一张，右侧半透明下一张滑到中间成为新的主图。
- 支持点击图片查看大图。
- 支持手动添加照片。

待确认：

- 轮播图展示的是菜品照片、家庭照片，还是两者都可以？
- 是否需要后台管理轮播图？
- “手动添加照片”是所有家庭成员都可以添加，还是只有管理员可以添加？

### 4.2 分类切换区

当前确定三个固定分类：

- 吃面！
- 吃米！
- 大餐！

分类含义：

- 吃面！：面条、粉、饺子、馄饨、包子等面食类。
- 吃米！：米饭、盖饭、炒饭、粥、饭团等米饭类。
- 大餐！：肉菜、硬菜、聚餐菜、想吃点好的。

布局：

- 三个横向按钮，在同一行展示。
- 当前选中分类使用蓝色。
- 未选中分类使用绿色。
- 按钮宽度根据文字长度自适应，但整体保持均衡。

交互：

- 点击分类后刷新下方菜品网格。
- 分类切换不清空已选择菜品。

已确认：

- 第一版分类固定为「吃面！」「吃米！」「大餐！」。

### 4.3 菜品卡片网格区

用途：展示当前分类下的菜品。每张菜品卡片必须展示真实菜品图片和菜名。菜品卡片不记录数量，点选后只表示“我想吃这个”。

布局：

- 每行 3 个卡片。
- 卡片为正方形浅蓝色圆角方块。
- 卡片内可展示菜名、图片、已选择状态。
- 菜品新增统一通过右侧或网格末尾的 `+` 按钮完成。

建议卡片内容：

- 真实菜品图片，必填
- 菜名，必填
- 选中状态

交互：

- 点击普通菜品卡片：选中/取消选中，表示“想吃/取消想吃”。
- 长按菜品卡片：进入编辑菜品页，可修改照片、名称和分类，也可删除菜品。

选中状态建议：

- 卡片边框加粗。
- 右上角出现小勾。
- 或卡片底部显示“想吃”。

已确认：

- 菜品照片和菜名必填。
- 长按菜品进入编辑，不再只是删除。

### 4.4 添加菜品按钮

草图中右侧有一个可爱的 `+` 号按钮。

用途：新增菜品。

布局：

- 放在菜品网格末尾。
- 去掉重边框，保持轻盈。
- `+` 号放大，作为明显但不笨重的新增入口。

交互建议：

点击后弹出新增菜品面板，包含：

- 上传/拍摄照片，必填
- 菜名，必填
- 分类，默认使用当前所在分类
- 保存

交互细节：

- 在「吃面！」分类下点击 `+`，新增菜品默认归入「吃面！」。
- 如果用户选错分类，可以长按对应菜品进入编辑页，修改分类。
- 未上传照片或未填写名称时，保存按钮不可用。

待确认：

- 是否需要支持拍照识别菜名？
- 新增时除了默认当前分类，是否也要允许手动切换分类？

### 4.5 滚动浏览区

草图中原本标注“这里是翻页功能”，现调整为上下滑动浏览。

用途：当菜品较多时，通过自然上下滑动查看更多菜品。

方案：

- 首页整体支持上下滑动。
- 菜品网格随页面自然延展。
- 底部操作区可以固定在底部，方便随时查看已选内容和提交。

可选辅助：

- 当菜品很多时，分类栏可吸顶。
- 页面底部显示“没有更多啦”之类的轻提示。

已确认：

- 使用上下滑动，不做翻页按钮。

### 4.6 底部操作区

包含两个主要操作：

- 心愿单
- 提交

心愿单按钮：

- 显示已选择菜品数量，这里的数量指“已选菜品个数”，不是每道菜的份数。
- 点击进入心愿单页或弹出心愿单抽屉。

提交按钮：

- 将当前选择提交为一次家庭点餐记录。
- 提交后通过微信订阅消息通知“大厨”，即用户本人。
- 提交后进入确认页或显示成功提示。

建议交互：

- 未选择任何菜品时，提交按钮置灰。
- 已选择菜品后，心愿单按钮显示数量角标。
- 点击提交前可展示确认弹窗。

待确认：

- 是否需要记录每个家庭成员是谁点的？
- 提交后是否还能修改？

### 4.7 微信订阅通知

用途：当老婆或其他家庭成员选完菜并提交后，大厨在微信中直接收到通知。

通知方式：

- 使用微信小程序订阅消息。
- 提交前需要用户授权订阅消息。
- 提交成功后，后端调用微信订阅消息接口发送通知给大厨。

通知内容建议：

- 标题：新的家庭点餐
- 点餐人：提交者昵称
- 菜品：已选菜品名称摘要
- 时间：提交时间
- 备注：可选

交互建议：

- 第一次提交时，引导点餐人授权订阅消息。
- 如果未授权，仍然允许提交，但页面内提示“已提交，微信通知可能无法送达”。
- 大厨需要在系统中绑定为接收通知的人。

待确认：

- 大厨是否永远只有你一个人？
- 是否需要支持多个大厨同时接收通知？
- 是否允许点餐人填写一句备注，比如“今晚想吃清淡一点”？

## 5. 页面状态

首页至少需要以下状态：

- 默认状态：展示轮播、分类和菜品。
- 空分类状态：当前分类暂无菜品，显示添加提示。
- 加载状态：菜品数据加载中。
- 已选择状态：部分菜品已标记为想吃。
- 提交成功状态：显示温柔的成功提示。

空状态文案示例：

- 这里还没有菜菜，先加一道吧
- 今天想吃什么？
- 给 HomeBite 添一道新菜

## 6. 信息架构建议

### 6.1 首页

用于浏览和选择菜品。

### 6.2 新增/编辑菜品页

字段建议：

- 菜名，必填
- 分类，必填
- 图片，必填
- 备注
- 是否常吃

编辑能力：

- 修改菜名
- 更换图片
- 修改分类
- 删除菜品

### 6.3 心愿单页

展示已选择菜品。

功能：

- 删除菜品
- 添加备注
- 提交点餐

### 6.4 历史记录页

可选功能。

用途：

- 查看之前点过什么。
- 快速复用上次菜单。

## 7. 组件规范

### 7.1 按钮

- 高度建议：44px 到 52px。
- 圆角建议：14px 到 18px。
- 主按钮使用浅蓝色。
- 次按钮使用白底深色描边。

### 7.2 卡片

- 菜品卡片使用 1:1 方形。
- 圆角建议：18px 到 22px。
- 图片铺满卡片上半部分或作为背景。
- 无图时使用浅蓝底和文字。

### 7.3 字体

- 中文字体建议使用系统默认圆润风格。
- 小程序中可优先使用 `PingFang SC` / 系统字体。
- 标题 18px 到 22px。
- 正文 14px 到 16px。
- 按钮文字 16px 到 18px。

## 8. 多家庭与登录权限

如果 HomeBite 未来给其他家庭使用，需要支持多家庭空间。每个用户通过微信登录进入小程序后，只能看到自己加入的家庭菜单；不同家庭之间的数据完全隔离。

### 8.1 登录方式

推荐使用微信小程序登录能力：

- 用户打开小程序后，通过微信登录获取用户身份标识。
- 后端保存用户的 `openid`。
- 用户可以加入一个或多个家庭。
- 当前选中的家庭决定首页展示哪一套菜单。

第一版可以默认每个用户只加入一个家庭，后续再支持切换家庭。

### 8.2 家庭空间

每个家庭拥有独立数据：

- 菜品库
- 点餐记录
- 成员列表
- 大厨通知接收人
- 轮播图

所有菜品、订单、照片都必须带 `familyId`，避免不同家庭之间串数据。

### 8.3 成员角色

建议角色：

- owner：家庭创建者，拥有全部权限。
- chef：大厨，可以收到订阅消息、管理菜单。
- editor：可新增和编辑菜单。
- member：可点菜和提交订单。

权限建议：

- owner：管理成员、设置大厨、编辑菜单、删除菜品。
- chef：编辑菜单、接收通知、查看订单。
- editor：新增/编辑/删除菜品。
- member：浏览菜单、选择想吃、提交点餐。

你的家庭场景中：

- 你可以是 owner + chef。
- 老婆可以是 member，也可以给 editor 权限，让她能维护菜品。

### 8.4 家庭成员添加

微信小程序通常不能直接读取并展示用户的微信好友列表，因此不建议做“从好友列表里勾选添加”。

推荐做法：

- owner 在成员管理页点击“邀请家人”。
- 小程序生成带 `familyId` 或 `inviteCode` 的邀请链接/分享卡片。
- owner 分享给微信好友。
- 对方从分享卡片进入小程序。
- 系统识别邀请参数，展示“加入 XX 的家庭菜单”。
- 对方确认后成为家庭成员。

邀请安全建议：

- 邀请码设置有效期。
- 邀请码只能使用一次，或限制使用次数。
- 加入后默认角色为 member。
- owner 可在成员管理页调整角色。

### 8.5 多家庭切换

如果一个用户加入多个家庭：

- 首页顶部可显示当前家庭名。
- 点击家庭名进入家庭切换页。
- 切换后重新加载对应家庭的菜品和点餐记录。

第一版可以先不做多家庭切换，只保留数据模型上的 `familyId`，为后续扩展留口。

## 9. 数据模型初稿

### 9.1 用户 User

字段：

- `id`
- `openid`
- `nickname`
- `avatarUrl`
- `createdAt`
- `lastLoginAt`

### 9.2 家庭 Family

字段：

- `id`
- `name`
- `ownerUserId`
- `chefUserIds`
- `createdAt`
- `updatedAt`

### 9.3 家庭成员 FamilyMember

字段：

- `id`
- `familyId`
- `userId`
- `role`
- `joinedAt`
- `status`

### 9.4 家庭邀请 FamilyInvite

字段：

- `id`
- `familyId`
- `inviteCode`
- `createdBy`
- `expiresAt`
- `maxUses`
- `usedCount`
- `status`

### 9.5 菜品 Dish

字段：

- `id`
- `familyId`
- `name`
- `category`
- `imageUrl`
- `note`
- `createdAt`
- `updatedAt`

可选字段：

- `favorite`
- `sortOrder`
- `lastOrderedAt`

校验规则：

- `name` 必填。
- `imageUrl` 必填。
- `category` 必须是 `noodle`、`rice`、`feast` 三者之一。

### 9.6 心愿单 WishlistItem

字段：

- `dishId`
- `name`
- `imageUrl`
- `note`

### 9.7 点餐记录 Order

字段：

- `id`
- `familyId`
- `items`
- `createdBy`
- `createdAt`
- `status`
- `note`

### 9.8 订阅通知 SubscriptionNotification

字段：

- `id`
- `familyId`
- `orderId`
- `receiverOpenId`
- `templateId`
- `status`
- `sentAt`
- `failReason`

## 10. 待确认问题汇总

1. 第一版是否就支持多家庭，还是先只做你家，但数据模型预留 `familyId`？
2. 家庭成员角色是否采用 owner / chef / editor / member？
3. 老婆是否需要编辑菜单权限，还是只负责点菜？
4. 轮播图的内容到底是什么：菜品推荐、猫咪照片、家庭照片，还是都可以？
5. 新增时除了默认当前分类，是否也要允许手动切换分类？
6. 大厨是否永远只有你一个人？
7. 是否需要支持多个大厨同时接收微信订阅消息？
8. 是否允许点餐人填写一句备注，比如“今晚想吃清淡一点”？
9. 是否需要记录每个家庭成员是谁点的？
10. 提交后是否还能修改？
11. 是否需要历史菜单和“一键再来一单”？
12. 是否需要后端和数据库？微信订阅消息通常需要后端或云函数。
13. 成员邀请是否采用微信分享卡片 + 邀请码？
14. UI 风格是更接近手绘草图，还是开发时做成干净圆润的正式 App 风？

## 11. MVP 建议

第一版建议只做：

- 微信登录
- 创建一个家庭空间
- 首页菜品展示
- 分类切换
- 新增菜品，照片和名称必填
- 长按编辑菜品
- 点选“想吃”
- 心愿单确认
- 提交生成一条点餐记录
- 微信订阅消息通知大厨
- 通过分享邀请家庭成员加入

暂时不做：

- 复杂权限
- 价格系统
- 多家庭切换
- 智能识别
- 复杂通知

这样可以先把家庭点餐的核心体验跑起来，再慢慢加可爱的小细节。
